spring.cache.type: none
spring:
  test:
    database:
      replace: none
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:jira-test;NON_KEYWORDS=VALUE
  liquibase:
    change-log: changelog_h2.sql